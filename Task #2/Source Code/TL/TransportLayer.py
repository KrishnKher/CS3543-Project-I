import socket
import logging
import time
import argparse
import threading


def test():
    print("It works!")



class RDT:

    def __init__(self):


    def flow_control(self, args):


    def congestion_control(self, args): # Thinking of 2 approaches.

    # Security, if time permits and if doable.

class Packet(RDT):

    def __init__(self):

    def packet_acknowledgement(self, packet):

    def packet_loss_detection(self, packet):

    def packet_retransmission(self, packet):

